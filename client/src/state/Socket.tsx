import { atom, selector } from "recoil";

export const NoticeState = atom<string[] | undefined>({
  key: "NoticeState",
  default: [],
});

// 임시
export const Nasfsd = atom<string[] | undefined>({
  key: "NoticeState",
  default: [],
});
